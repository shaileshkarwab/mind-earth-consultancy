import { AppConfigConstants } from './app-config-constants';

describe('AppConfigConstants', () => {
  it('should create an instance', () => {
    expect(new AppConfigConstants()).toBeTruthy();
  });
});
