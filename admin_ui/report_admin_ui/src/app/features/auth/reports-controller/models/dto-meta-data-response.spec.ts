import { DtoMetaDataResponse } from './dto-meta-data-response';

describe('DtoMetaDataResponse', () => {
  it('should create an instance', () => {
    expect(new DtoMetaDataResponse()).toBeTruthy();
  });
});
