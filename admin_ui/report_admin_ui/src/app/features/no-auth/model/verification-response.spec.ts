import { VerificationResponse } from './verification-response';

describe('VerificationResponse', () => {
  it('should create an instance', () => {
    expect(new VerificationResponse()).toBeTruthy();
  });
});
